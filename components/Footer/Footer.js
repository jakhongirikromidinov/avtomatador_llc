import StyledFooter, { Content } from "./Footer.styled";
import { Container } from "../Common/Common.styled";

const Footer = () => {
	return (
		<StyledFooter>
			<Container>
				<Content></Content>
			</Container>
		</StyledFooter>
	);
};

export default Footer;
